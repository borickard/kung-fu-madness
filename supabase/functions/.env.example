# Optional. When set, sweep-deadlines also accepts x-sweep-secret instead of
# the service role key. SUPABASE_URL, SUPABASE_ANON_KEY and
# SUPABASE_SERVICE_ROLE_KEY are injected by the CLI; do not put them here.
SWEEP_SECRET=
