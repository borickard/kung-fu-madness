# Local Supabase, printed by `pnpm supabase start`. The anon key is now called
# the "Publishable" key (sb_publishable_...); the "Secret" key never goes here.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=replace-me
