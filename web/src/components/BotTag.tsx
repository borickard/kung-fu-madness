/** Marks a fighter with nobody behind it. */
export function BotTag() {
  return (
    <span className="border-rule text-muted ml-2 border px-1 py-px text-[10px] tracking-[0.08em] uppercase">
      bot
    </span>
  );
}
